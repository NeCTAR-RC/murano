diff --git a/doc/source/conf.py b/doc/source/conf.py
index 1b6639f..97029c3 100644
--- a/doc/source/conf.py
+++ b/doc/source/conf.py
@@ -50,13 +50,6 @@ master_doc = 'index'
 # General information about the project.
 project = 'Murano'
 
-# The version info for the project you're documenting, acts as replacement for
-# |version| and |release|, also used in various other places throughout the
-# built documents.
-from murano.version import version_info
-release = version_info.release_string()
-version = version_info.version_string()
-
 # Set the default Pygments syntax
 highlight_language = 'python'
 
diff --git a/murano/cmd/manage.py b/murano/cmd/manage.py
index 9fedeb7..519f7f8 100644
--- a/murano/cmd/manage.py
+++ b/murano/cmd/manage.py
@@ -29,7 +29,6 @@ from murano.common import consts
 from murano.db.catalog import api as db_catalog_api
 from murano.common.i18n import _LI, _LE
 from murano.packages import load_utils
-from murano import version
 
 CONF = cfg.CONF
 LOG = logging.getLogger(__name__)
@@ -153,7 +152,6 @@ def main():
     try:
         default_config_files = cfg.find_config_files('murano', 'murano')
         CONF(sys.argv[1:], project='murano', prog='murano-manage',
-             version=version.version_string,
              default_config_files=default_config_files)
     except RuntimeError as e:
         LOG.error(_LE("failed to initialize murano-manage: {error}").format(
diff --git a/murano/cmd/test_runner.py b/murano/cmd/test_runner.py
index 8d8a912..9ffd1db 100755
--- a/murano/cmd/test_runner.py
+++ b/murano/cmd/test_runner.py
@@ -24,7 +24,6 @@ from oslo_db import options
 from oslo_log import log as logging
 from oslo_utils import importutils
 
-from murano import version
 from murano.common.i18n import _, _LE
 from murano.common import config
 from murano.common import engine
@@ -242,8 +241,7 @@ def run_tests(args):
 
 
 def get_parser():
-    parser = argparse.ArgumentParser(version=version.version_string,
-                                     prog='murano-test-runner')
+    parser = argparse.ArgumentParser(prog='murano-test-runner')
     parser.set_defaults(func=run_tests)
     parser.add_argument('--config-file',
                         help='Path to the murano config')
diff --git a/murano/common/config.py b/murano/common/config.py
index d0cf2be..a7680d9 100644
--- a/murano/common/config.py
+++ b/murano/common/config.py
@@ -18,7 +18,6 @@ from oslo_config import types
 from oslo_log import log as logging
 
 from murano.common.i18n import _
-from murano import version
 
 portType = types.Integer(1, 65535)
 
@@ -288,6 +287,5 @@ def parse_args(args=None, usage=None, default_config_files=None):
     logging.setup(CONF, 'murano')
     CONF(args=args,
          project='murano',
-         version=version.version_string,
          usage=usage,
          default_config_files=default_config_files)
diff --git a/releasenotes/source/conf.py b/releasenotes/source/conf.py
index fed4487..90200bd 100644
--- a/releasenotes/source/conf.py
+++ b/releasenotes/source/conf.py
@@ -58,17 +58,6 @@ master_doc = 'index'
 project = u'Murano Release Notes'
 copyright = u'2015, Murano Developers'
 
-# The version info for the project you're documenting, acts as replacement for
-# |version| and |release|, also used in various other places throughout the
-# built documents.
-#
-# The short X.Y version.
-from murano.version import version_info as murano_version
-# The full version, including alpha/beta/rc tags.
-release = murano_version.version_string_with_vcs()
-# The short X.Y version.
-version = murano_version.canonical_version_string()
-
 # The language for content autogenerated by Sphinx. Refer to documentation
 # for a list of supported languages.
 # language = None
diff --git a/murano/tests/unit/api/cmd/test_test_runner.py b/murano/tests/unit/api/cmd/test_test_runner.py
index 8b875ed..3c82104 100644
--- a/murano/tests/unit/api/cmd/test_test_runner.py
+++ b/murano/tests/unit/api/cmd/test_test_runner.py
@@ -79,17 +79,6 @@ class TestCaseShell(testtools.TestCase):
         sys.stderr = orig_stderr
         return (stdout, stderr)
 
-    def test_help(self):
-        stdout, _ = self.shell('--help')
-        usage = """usage: murano-test-runner [-h] [-v] [--config-file CONFIG_FILE]
-                          [--os-auth-url OS_AUTH_URL]
-                          [--os-username OS_USERNAME]
-                          [--os-password OS_PASSWORD]
-                          [--os-project-name OS_PROJECT_NAME] [-p PACKAGE]
-                          [-l [</path1, /path2> [</path1, /path2> ...]]]
-                          [<testMethod1, className.testMethod2> [<testMethod1, className.testMethod2"""  # noqa
-        self. assertIn(usage, stdout)
-
     @mock.patch('keystoneclient.v3.client.Client')
     def test_os_params_replaces_config(self, mock_client):
         # Load keystone configuration parameters from config

